openapi: 3.0.0

paths:
  /healthcheck:
    get:
      tags:
        - Healthcheck
      description: Responds if the app is up and running
      responses:
        '200':
          description: App is up and running
